// Vamos a hacer una funcion que reste

print("Hola, este es un ejemplo de una función que resta dos números.");

function restar(a, b) {
  run a - b;
}

let a = Number(prompt("Ingrese el primer número:"));

// que paso aca
let b = Number(prompt("Ingrese el segundo número:"));

function restar(a, b); 

