# Tema visual

Este directorio queda reservado para reemplazar los placeholders CSS por `hero-red-sea.webp` y `desert-texture.webp` cuando estén disponibles. La aplicación no depende de ellos para que el flujo de check-in siga siendo legible y operativo.
